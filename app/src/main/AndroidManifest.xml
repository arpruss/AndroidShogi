<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mobi.omegacentauri.shogi"
    android:label="AndroidShogi"
    android:versionCode="353"

    android:versionName="3.53">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:icon="@drawable/kanji_light_threedim_hi"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".StartScreenActivity"
            android:alwaysRetainTaskState="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GameActivity"
            android:label="@string/app_name"
            ></activity>
        <activity
            android:name=".ReplayGameActivity"
            android:label="@string/app_name"
            ></activity>
        <activity
            android:name=".GameLogListActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".ShogiPreferenceActivity"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".HelpActivity"
            android:label="@string/app_name"></activity>
    </application>


</manifest>
